import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Towers of Hanoi - Interactive Puzzle Game",
  description: "Play the classic Towers of Hanoi puzzle game with customizable difficulty and auto-solve feature.",
  keywords: "towers of hanoi, puzzle game, algorithm, recursive, brain teaser",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
